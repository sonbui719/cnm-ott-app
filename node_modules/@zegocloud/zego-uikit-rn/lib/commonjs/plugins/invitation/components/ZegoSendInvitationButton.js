"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = ZegoSendInvitationButton;
var _react = _interopRequireWildcard(require("react"));
var _reactNative = require("react-native");
var _services = _interopRequireDefault(require("../services"));
var _ZegoInvitationType = _interopRequireDefault(require("./ZegoInvitationType"));
var _logger = require("../../../utils/logger");
var _ZegoUIKitInternal = _interopRequireDefault(require("../../../components/internal/ZegoUIKitInternal"));
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
function _getRequireWildcardCache(e) { if ("function" != typeof WeakMap) return null; var r = new WeakMap(), t = new WeakMap(); return (_getRequireWildcardCache = function (e) { return e ? t : r; })(e); }
function _interopRequireWildcard(e, r) { if (!r && e && e.__esModule) return e; if (null === e || "object" != typeof e && "function" != typeof e) return { default: e }; var t = _getRequireWildcardCache(r); if (t && t.has(e)) return t.get(e); var n = { __proto__: null }, a = Object.defineProperty && Object.getOwnPropertyDescriptor; for (var u in e) if ("default" !== u && {}.hasOwnProperty.call(e, u)) { var i = a ? Object.getOwnPropertyDescriptor(e, u) : null; i && (i.get || i.set) ? Object.defineProperty(n, u, i) : n[u] = e[u]; } return n.default = e, t && t.set(e, n), n; }
let requesting = false;
function ZegoSendInvitationButton(props) {
  const {
    icon,
    text,
    invitees = [],
    type = _ZegoInvitationType.default.videoCall,
    data = '',
    onRequestData,
    timeout = 60,
    onPressed,
    onFailure,
    onWillPressed,
    onRequestStatusChanged,
    backgroundColor = '#F3F4F7',
    fontSize = 16,
    color = '#2A2A2A',
    width = 42,
    height = 42,
    borderRadius = 1000,
    borderWidth,
    borderColor,
    borderStyle,
    verticalLayout,
    // Default row layout, no layout parameters default to precedence icon
    resourceID,
    notificationTitle,
    notificationMessage
  } = props;
  const getImageSourceByPath = () => {
    if (type === _ZegoInvitationType.default.videoCall) {
      return require('../resources/blue_button_video_call.png');
    } else {
      return require('../resources/blue_button_audio_call.png');
    }
  };
  const getRenderView = () => {
    let renderView;
    if (verticalLayout === undefined) {
      // Choose between icon and text
      if (icon) {
        renderView = /*#__PURE__*/_react.default.createElement(_reactNative.Image, {
          resizeMode: "contain",
          source: icon
        });
      } else {
        if (!text) {
          renderView = /*#__PURE__*/_react.default.createElement(_reactNative.Image, {
            resizeMode: "contain",
            source: getImageSourceByPath()
          });
        } else {
          renderView = /*#__PURE__*/_react.default.createElement(_reactNative.Text, {
            style: getCustomTextStyle(fontSize, color).text
          }, text);
        }
      }
    } else {
      // Both icon and text exist
      var marigin = text ? 6 : 0;
      renderView = /*#__PURE__*/_react.default.createElement(_react.Fragment, null, /*#__PURE__*/_react.default.createElement(_reactNative.Image, {
        resizeMode: "contain",
        source: icon || getImageSourceByPath(),
        style: {
          marginRight: marigin
        }
      }), /*#__PURE__*/_react.default.createElement(_reactNative.Text, {
        style: getCustomTextStyle(fontSize, color).text
      }, text));
    }
    return renderView;
  };
  const getCustomTextStyle = (fontSize, color) => _reactNative.StyleSheet.create({
    text: {
      fontSize,
      color
    }
  });
  const getCustomContainerStyle = () => _reactNative.StyleSheet.create({
    customContainer: {
      flexDirection: verticalLayout ? 'column' : 'row',
      width: width,
      height: height,
      backgroundColor: backgroundColor,
      borderRadius: borderRadius,
      borderWidth: borderWidth,
      borderColor: borderColor,
      borderStyle: borderStyle
    }
  });
  const onButtonPress = () => {
    if (requesting) {
      (0, _logger.zloginfo)('[Components]Send invitation requesting..... return.');
      callOnFailure({
        code: -1,
        message: 'Press too frequently.'
      });
      return;
    }
    if (invitees.length == 0) {
      callOnFailure({
        code: -1,
        message: 'Invitees is empty.'
      });
      return;
    }
    requesting = true;
    typeof onRequestStatusChanged === 'function' ? onRequestStatusChanged(requesting) : null;
    let canSendInvitation = true;
    if (onWillPressed) {
      if (typeof onWillPressed === 'function') {
        (0, _logger.zloginfo)('#########onWillPressed function', onWillPressed);
        canSendInvitation = onWillPressed();
      } else {
        // don't support promise
        canSendInvitation = false;
      }
    }
    (0, _logger.zloginfo)('#########onWillPressed result:', canSendInvitation);
    if (!canSendInvitation) {
      requesting = false;
      typeof onRequestStatusChanged === 'function' ? onRequestStatusChanged(requesting) : null;
      return;
    }
    let updatedData = null;
    if (typeof onRequestData === 'function') {
      updatedData = onRequestData();
    } else if (data != undefined) {
      updatedData = data;
    } else {
      requesting = false;
      typeof onRequestStatusChanged === 'function' ? onRequestStatusChanged(requesting) : null;
      return;
    }
    let notificationConfig = {
      resourceID,
      title: notificationTitle,
      message: notificationMessage
    };
    (0, _logger.zloginfo)(`[Components]Send invitation start, invitees: ${JSON.stringify(invitees)}, timeout: ${timeout}, type: ${type}, data: ${updatedData}, notificationConfig: ${JSON.stringify(notificationConfig)}`);
    _services.default.sendInvitation(invitees, timeout, type, updatedData, notificationConfig).then(({
      code,
      message,
      callID,
      errorInvitees
    }) => {
      (0, _logger.zloginfo)(`[Components]Send invitation success, code: ${code}, message: ${message}, errorInvitees: ${errorInvitees}`);
      if (invitees.length > errorInvitees.length) {
        if (typeof onPressed === 'function') {
          const inviteesBackup = JSON.parse(JSON.stringify(invitees));
          errorInvitees.forEach(errorInviteeID => {
            const index = inviteesBackup.findIndex(inviteeID => errorInviteeID === inviteeID);
            index !== -1 && inviteesBackup.splice(index, 1);
          });
          onPressed({
            invitationID: callID,
            errorCode: code,
            errorMessage: message,
            errorInvitees,
            invitees: inviteesBackup
          });
        }
      }
      setTimeout(() => {
        requesting = false;
        typeof onRequestStatusChanged === 'function' ? onRequestStatusChanged(requesting) : null;
      }, 1000);
    }).catch(({
      code,
      message
    }) => {
      _ZegoUIKitInternal.default.notifyErrorUpdate('SendInvitation', code, message);
      callOnFailure({
        code,
        message
      });
      setTimeout(() => {
        requesting = false;
        typeof onRequestStatusChanged === 'function' ? onRequestStatusChanged(requesting) : null;
      }, 1000);
      (0, _logger.zlogerror)(`[Components]Send invitation error, code: ${code}, message: ${message}`);
    });
  };
  const callOnFailure = ({
    code,
    message
  }) => {
    if (typeof onFailure === 'function') {
      onFailure({
        code: code,
        message: message
      });
      (0, _logger.zloginfo)('[ZegoSendInvitationButton][onButtonPress] onFailure execute end');
    }
  };
  (0, _react.useEffect)(() => {
    (0, _logger.zloginfo)(`[ZegoSendInvitationButton] useEffect, notificationTitle: ${notificationTitle}`);
    return () => {
      (0, _logger.zloginfo)(`[ZegoSendInvitationButton] useEffect return`);
    };
  }, []);
  return /*#__PURE__*/_react.default.createElement(_reactNative.TouchableOpacity, {
    style: [styles.container, getCustomContainerStyle().customContainer],
    onPress: onButtonPress
  }, getRenderView());
}
const styles = _reactNative.StyleSheet.create({
  container: {
    alignItems: 'center',
    justifyContent: 'center'
  }
});
//# sourceMappingURL=ZegoSendInvitationButton.js.map